/*const API_URL = 'https://udemy-test-3353a.firebaseio.com/users.json';

interface UserData {
    name: string;
    email: string;
    password: string;
    company?: {
        name: string;
    }
}
*/
/*
export const getUsers = () => {
    return fetch( API_URL, {
        headers: {
            'Content-Type': 'application/json'
        }
    }).then( res => res.json() );
}

export const postUser = ( userData: any ) => {
    return fetch( API_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify(userData)
    }).then( res => res.json() );
}*/